public class Strings {
    public static void  main (String [] arg){
        String postreFavorito = "Torta de Chocolate";
        System.out.println(postreFavorito.toLowerCase());
        System.out.println(postreFavorito.contains("torta"));
        System.out.println(postreFavorito.toUpperCase().contains("torta".toUpperCase()));
    }
}
